<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="MapsActivity_Menu_Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/veliby_purple</item>
        <item name="android:padding">7sp</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="MapsActivity_Menu_Button.Pressed">
        <item name="android:background">@color/menu_button_pressed</item>
        <item name="android:textColor">@color/menu_button_pressed_text</item>
    </style>

    <style name="MapsActivity_Menu_Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">2dp</item>
        <item name="android:background">@color/veliby_purple</item>
        <item name="android:layout_margin">5sp</item>
    </style>

    <style name="MapsActivity_Info_Divider">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">1dp</item>
        <item name="android:background">@color/veliby_purple_light</item>
        <item name="android:layout_margin">5sp</item>
    </style>
</resources>
